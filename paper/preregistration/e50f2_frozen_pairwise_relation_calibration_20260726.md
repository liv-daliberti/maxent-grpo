# E50F2 — frozen E47W pairwise relation calibration on sound route pairs

**Status: PREREGISTERED 2026-07-26 after E50F failed, before E49AB terminal
output**

## Why E50F does not authorize anything

E50F rejected the newly written menu-level relation prompt because it split
four of ten manually equivalent controls.  Two involved routes whose claimed
methods were not actually executed, and two sound controls were merely
tree/tuple renderings of the same product count.  Its result remains a
failure and is not reused.

The online canonicalizer validates derivations before asking whether two
valid responses are strategically distinct.  E50F2 therefore restricts the
same blinded E49R cohort to the 18 pairs manually labeled with *both* routes
sound and self-contained: 12 distinct and six equivalent.

## Frozen classifier

Use the exact E47W pairwise-veto source already calibrated with:

- zero false-new errors on 150 exact/format/paraphrase injections;
- zero false-new errors on nine manually same pairs;
- all semantic regressions passing, including a different-proof split and
  routine-equivalence merges.

Do not write a new relation prompt.  Invoke its private
`_judge_relations` path exactly, including its messages, schema, two
deterministic seeds, permutation logic, and fail-closed parser.  Render each
manual route only as its plan, exact action combo, and declared operations;
do not expose labels, rationales, reference answers, or execution verdicts.

Use seeds 500771 and 500772.  Each call contains one opaque pair for its
original problem.  Preserve the exact request, response ID, response content
hash, and parsed relation.

## Gate

Pass only with:

1. zero false-new decisions for all six equivalent pairs in both passes;
2. at most two false merges among the 12 distinct pairs in each pass;
3. unanimous decisions across both passes for every pair;
4. no request, schema, ID-coverage, or finish failure.

Only a passing E50F2 result may become the relation component of a prospective
post-validation route audit.  It does not alter any predecessor result.
