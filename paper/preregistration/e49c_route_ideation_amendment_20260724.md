# E49C distinct-route ideation amendment — 2026-07-24

**Status: FROZEN BEFORE ANY E49C TRAINING LAUNCH**

The E49C treatment, canonical key, exact execution gate, two frozen audit
seeds, matched control, hyperparameters, datasets, and advancement criteria
remain unchanged.

The frozen one-shot generator failed closed on four of its first ten toy rows.
The auditors correctly rejected fake distinctions: an irrelevant negative
quadratic root, repeated prime factorization, a redundant cone-area
afterthought, and duplicate action combos. No failed menu was admitted.

For rows still rejected after four ordinary attempts, the offline
materializer may now run up to four rescue attempts. Each rescue uses the same
frozen Qwen2.5-72B at temperature zero to:

1. propose exactly two sound route families with different decisive methods,
   without access to the reference answer;
2. compile those two routes into S1/S2 with at least one route-specific action
   each; and
3. submit the resulting menu to the unchanged two independent soundness and
   pairwise-distinctness audits.

General method-family examples in the ideation prompt are problem-agnostic
(e.g. CRT versus enumeration, quadratic versus factor-pair search,
arc-length versus area, direct versus complementary counting). They are not
accepted as evidence. A row still receives no menu unless both original
auditors unanimously mark every compiled strategy sound and every pair
distinct. All original failed attempts and rescue prompts are retained in the
durable row record.
