# E49E answer-blind singleton V2b node915 relaunch

**Status: FROZEN BEFORE ANY V2B PROPOSAL OR AUDIT REQUEST — 2026-07-24**

V2b repeats the already frozen answer-blind symbolic-contract procedure after
the conservative abandonment of the node302 V2 placement.  It changes only:

- repair version:
  `e49e_answer_blind_symbolic_singleton_repair_v2b`;
- fixed proposal seeds: `492251`, `492252`, `492253`, `492254`;
- CPU-only placement: mltheory node915.

The four role instructions, problem-only request surface, JSON schema,
numeric and number-word leakage gates, two soundness auditors, exact-answer
validator, first-double-sound rule, four-row concurrency, carry-only handling
of completed V1 successes, and singleton-only semantics are unchanged.

The abandoned V2 evidence and its terminal-abort amendment are bound into the
V2b frozen identity.  V2b neither reads nor reuses any possible in-flight V2
response.
