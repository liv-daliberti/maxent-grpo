# E47W-CAL: reasoned pairwise novelty-veto calibration

**Status: FROZEN BEFORE LAUNCH — 2026-07-24**

E47R passed every automatic duplicate/paraphrase check but failed its blinded
semantic audit: 2 of 11 analyst-same pairs received different keys (18.2%).
The failures were context-induced splits of:

- two solutions using the identical median/Apollonius identity; and
- circumference-times-revolutions versus the equivalent speed-times-time
  parameterization of the same clock-tip calculation.

E47S, E47T, and E47U are preserved failed repair preflights. Cluster-shaped
novelty vetoes either still split a routine calculation or erased the frozen
coefficient-expansion versus strategic-point-evaluation boundary, sometimes
while their own prose explicitly named those different operations.

E47V is the first repair preflight to pass. The executable keeps E47R's two
independent derivation-integrity checks and two full-context boundary
partitions. It then reduces every proposed candidate component to one
representative and asks two independently permuted, schema-bound pairwise
audits for:

`{pair_id, brief_check, relation in [same,different,ambiguous]}`.

Any `same` decision conservatively coarsens a proposed boundary. A boundary
can create novelty only when neither pairwise audit says `same` or
`ambiguous`. More than 128 required reduced comparisons fails closed for new
keys. Existing full-context matches remain reusable. The persistent state
schema is `math_strategy_canonicalizer_pair_veto_v15`.

E47W is the full 50-problem replay of that exact executable over the unchanged
3,200 Qwen0.5B samples, final-answer validator outcomes, injected controls,
group-16 online schedule, Qwen72 checkpoint, seeds, temperature, and bounded
JSON protocol. Its preflight must rerun and pass:

- valid-anchor admission;
- both known invalid modular derivations rejected;
- the direct routine clock pair merged;
- the exact seven-response E47R clock context prevented from false splitting;
- the exact sixteen-response E47R median context prevented from false
  splitting; and
- coefficient expansion kept distinct from strategic point evaluation.

E47W advances only if:

- exact-duplicate false-new is zero;
- overall injected false-new is at most 5%;
- lexical-paraphrase false-new is at most 10%;
- at least 50% of answer-matched policy responses receive a key;
- both known invalid witnesses receive no key;
- blinded analyst-same false-new is at most 5%;
- blinded analyst-different false-merge is at most 20%, or the frozen valid
  different-route regression passes when no natural analyst-different pair
  exists;
- every E47V semantic regression passes; and
- every bounded problem completes without structural failure.

Labels must again be committed before the hidden pair-source key is opened.
Passing licenses only the already frozen E49B hard-toy pair.
