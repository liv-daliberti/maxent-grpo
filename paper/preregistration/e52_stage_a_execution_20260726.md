# E52 conditional three-seed Stage-A execution

**Status: FROZEN BEFORE STAGE-A AUTHORIZATION — 2026-07-26**

Stage A is the conditional replication already specified by the E52 parent
protocol. It may be submitted only after the exact three-domain, 50-pass
sentinel produces a machine-readable positive approval under the scale-free
stability amendment.

## Cohort

The cohort contains exactly 27 fresh jobs:

- graph coloring, Countdown easy3, and executable Python factors;
- matched Dr.GRPO, direct inverse conditional-token entropy, and direct
  inverse entropy plus fixed canonical shaping;
- independent seeds 43, 44, and 45.

Every optimizer, sampling, evaluation, response-length, checkpoint, and
50-pass setting is identical to the sentinel. No sentinel model checkpoint,
optimizer, entropy EMA/reference, canonical bank, rollout, or random-number
state is resumed.

## Immutable authorization

Before submission, the launcher must replay a positive terminal approval and
verify all of the following byte identities:

- sentinel identity and parent/repair protocols;
- scale-free stability amendment;
- frozen training source and execution snapshots;
- sentinel launcher and terminal auditor;
- the approval file itself.

The approval must cover all nine sentinel runs at exact terminal evaluation,
with every domain's runtime, safety, original behavioral, multiplicity, and
self-retention checks passing. Missing, pending, failed, edited, or partially
covered evidence authorizes no Stage-A job.

All 27 jobs are submitted held. The launcher checks the exact
domain/arm/seed Cartesian product and the treatment environment of every held
job before releasing the cohort atomically. Any incomplete or mismatched held
cohort is cancelled.

A separately held, hash-bound CPU sidecar is released with the cohort and
audits all 27 runs for four days. It applies the per-run controller/runtime
checks, the complete terminal gate independently to every seed, and the same
scale-free behavioral gate to the three-seed mean trajectory. The sidecar
also republishes the current-canonical dashboard and 0--50-pass figure from
the fresh three-seed curves; heavy lines are seed means and faint lines are
individual seeds. Gold-support-normalized coverage is omitted from that
decision view.

## Final interpretation

Stage A is confirmatory rather than a coefficient search. Its results cannot
change the controller reference, canonical coefficient, novelty coefficient,
or any domain-specific target. Success requires the target-free stability
criteria to hold across the three fresh seeds and all three domains; absence
of crashes alone is insufficient.
