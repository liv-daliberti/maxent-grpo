# E49C conservative menu novelty-veto amendment — 2026-07-24

**Status: FROZEN BEFORE ANY E49C TRAINING LAUNCH**

A spot audit of the first preprocessing pass found that both menu auditors
called “compute the remainder by division” and “write the division algorithm
`n = qm + r`” distinct. This is a false-new boundary under the already frozen
E47W/E49C definition: routine algebra or a restatement of the same decisive
identity must merge. No E49C training had launched.

The menu pair audit is therefore versioned
`conservative_pairwise_novelty_veto_v2` and made explicitly asymmetric:

- equivalence is the default;
- both frozen audit passes must name different decisive central operations to
  authorize `distinct`;
- routine reparameterization, exponent-law restatement, division versus
  `n=qm+r`, reordered algebra, irrelevant checks, and redundant afterthoughts
  are explicitly equivalent;
- uncertainty vetoes novelty.

Examples of potentially real boundaries remain method-level: CRT versus
progression enumeration, factor-pair search versus the quadratic formula,
direct versus complementary counting, synthetic versus coordinate geometry,
recurrence versus a closed form, or independent area versus length
invariants—only when actually present and not routinely reducible to the same
central equation.

Every previously generated row is ineligible unless it is regenerated and
passes both v2 audits. No prior pass is grandfathered in. Soundness auditing,
execution validation, task-reward gating, canonical keys, training arms, and
all outcome gates are unchanged.
