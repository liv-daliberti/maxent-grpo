# E47P-REG: clean valid distinct-route regression

**Status: FROZEN BEFORE LAUNCH — 2026-07-24**

E47O again rejected the absolute-value case proof while its own brief check
repeated the correct discriminant, acknowledged that both roots violate the
assumed case, and nevertheless called this a material error. This item is a
pathological validity-classification probe and is retired from the distinct
route regression; E47O is preserved as failed.

E47P uses an existing level-3 problem from the exact OAT MATH train split:
shift \(y=2x^2-x+7\) four units right and find \(a+b+c\). Two manually
written responses are independently answer-validated:

1. substitute \(x-4\), expand, and sum the resulting coefficients;
2. use \(a+b+c=g(1)\), map through the shift, and evaluate \(f(-3)\).

These are unambiguously different central routes under the frozen definition.
The invalid-proof and routine-equivalence regression cases are unchanged.
The executable canonicalizer and prompts are unchanged from E47N/O.

E47P advances only if all four frozen live semantic checks pass.
