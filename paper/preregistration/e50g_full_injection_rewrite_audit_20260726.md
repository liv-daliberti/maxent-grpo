# E50G full-cohort rewrite audit

**Status: RETROSPECTIVE FROZEN-CODE AUDIT — 2026-07-26**

This audit was specified after an exploratory dry run reported its aggregate
counts.  It is therefore not claimed as an independently preregistered
calibration.  It is retained as a reproducible whole-cohort diagnostic.  The
signature source was already frozen at SHA-256
`f3455c3bd575a9c7e8cbb0b4bc1c5bad5fd7545418cca4e1a86423334aae08cc`;
neither its vocabulary nor safe-pair allowlist may change in response.

Use all 50 fixed level-5 E47 problems.  For each problem compare its official
anchor solution with its exact duplicate, whitespace/format variant, and
lexical paraphrase from the frozen blinded injection packet.  Treat the
complete solution text as a declared route action and call only the frozen
safe-signature boundary.  These 150 comparisons are known same-strategy
controls.  Report every signature and decision, grouped counts, signature
changes, and immutable input/output hashes.

The diagnostic passes only if:

- all 50 problems and exactly 150 comparisons are present;
- each injection kind has exactly 50 comparisons;
- no rewrite is classified as a safe distinct pair; and
- no rewrite changes either the full signature-hit set or decisive signature.

This audit cannot by itself authorize training because its outcome was seen
before this document was written.  E50G still requires its prospective
proposal-to-menu rendering controls, frozen hard-MATH manual-pair development
audit, two literal soundness/binding audits, and forced/unforced 0.5B runtime
gates.
