# PantryPlan paired mechanism smoke v1-r1: validator-only repair

**Status: FROZEN AFTER JOBS 30200705--30200707 FAILED BEFORE TRAINING AND BEFORE THE R1 PAIR — 2026-07-30**

## Immutable antecedent

Both v1 training jobs stopped in argument validation before model construction,
sampling, or an optimizer update. Their identical error was that the online
canonical validator recognized only the generic `qwen_boxed` ModeBench prompt.
The later compatibility check also prohibited an online canonical bank and
semantic Shannon term whenever canonical finite actions were active, despite
the task-bound Pantry six-bit verifier being the prospectively frozen surface.
Job 30200707 consequently produced a failed audit. These receipts remain the
v1 outcome; they are not relabeled or overwritten.

## R1 repair

R1 changes only the argument-admission contract. For
`canonical_action_task=pantry_support_mask`, the executable ModeBench contract
accepts exactly `qwen_pantry_support_mask`, `verifier_version=fast`, and
`test_split=multi_answer`. Only that task-bound canonical action surface may
compose with the online canonical bank and semantic Shannon mechanism. Other
canonical tasks retain the existing fail-closed exclusions.

The model, Pantry v2 data, seed 76201, pair of arms, 32 optimizer updates,
six-bit horizon, 16 rollouts, optimizer, coefficients, replay traversal,
compute-matched control, pass criteria, and ten-job authorization boundary are
unchanged from v1. R1 snapshots the repaired source before submission. No v1
sample, checkpoint, prompt, metric, or threshold is reused as an outcome.

## Decision

The independent r1 audit applies the unchanged v1 scientific criteria. A pass
authorizes exactly the ten Pantry Stage-B jobs. Any scientific failure stops
Pantry; only a demonstrated pre-training infrastructure failure may receive a
separately frozen repair.
