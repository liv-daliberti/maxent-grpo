# E50F3 — finite strategy-family canonicalization calibration

**Status: PREREGISTERED 2026-07-26 after E50F/E50F2 failed and before E49AC
terminal output**

## Motivation

Both a newly written relation prompt (E50F) and the exact frozen E47W
pairwise-veto prompt (E50F2) split the same two sound equivalence controls:

- drawing a product-rule count as a tree versus writing the product; and
- filtering injective ordered tuples versus writing the falling factorial.

Those are false-new errors under the frozen manual strategy boundary.  Neither
failed classifier may authorize a route.

E50F3 removes the open-ended binary relation judgment.  After exact execution
soundness is established, Qwen2.5-72B maps each route to one member of a
finite, deliberately coarse strategy-family ontology.  Equality of canonical
families is then deterministic.

## Finite ontology

The ontology distinguishes central mathematical engines such as:

- polynomial expansion and strategic value/invariant evaluation;
- quadratic/root formula and Vieta/coefficient relations;
- Euclidean algorithm, prime factorization, CRT construction, and residue
  analysis;
- calculus and inequality/extremal bounds;
- finite candidate search, dynamic programming, and closed forms;
- direct product-style counting, incidence/double counting, complementary
  counting, and inclusion-exclusion/direct unions;
- coordinate/vector, synthetic, and trigonometric geometry; and
- exact/numerical representation comparison.

Tree, grid, table, loop, tuple, and falling-factorial renderings of the same
Cartesian-product or injective-assignment count are all
`direct_combinatorial_count`.  `finite_candidate_search` is reserved for
generating candidates and testing an independent constraint.  Decimal versus
common-denominator comparison is one representation-comparison family.

No open-set family is allowed.  `other` is a conservative catch-all and can
never establish novelty against another `other`.

## Blinded calibration

Use the same 18 E49R pairs manually labeled with both routes sound and
self-contained: 12 distinct and six equivalent.  Render only problem, plan,
exact combo, and declared operations.  Do not expose labels, rationales,
answers, or execution verdicts.

Run two deterministic, independently permuted assignments with seeds 500781
and 500782.  Each pass classifies all 36 routes.  A pair is different only
when both passes assign stable, non-`other`, unequal families.  Otherwise it
is same/uncertain and cannot create novelty.

Pass only with:

1. zero false-new decisions on all six equivalent pairs;
2. at most two false merges on the 12 distinct pairs;
3. identical family assignment across passes for every route;
4. complete, unique, terminal, well-formed judge output.

Only a passing result may define prospective menu relation.  Per-response
online credit still requires exact answer validation and exact finite-menu
route execution; the family label itself never earns reward.
