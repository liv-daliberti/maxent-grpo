# E47Q-REG: explicit distinct-boundary regression

**Status: FROZEN BEFORE LAUNCH — 2026-07-24**

E47P accepted both valid graph-shift proofs but merged them. E47P is preserved
as a strategy-boundary preflight failure.

E47Q changes only the general strategy-equivalence instruction. Alongside its
existing examples, it states that explicitly expanding coefficients is
different from avoiding expansion by evaluating a function at a strategically
chosen point. Routine unit changes and algebraic reassociation remain the same
route. The persistent schema is
`math_strategy_canonicalizer_reasoned_equivalence_v13`.

The proof-integrity prompt, all admission logic, and all four frozen live
regression cases are unchanged. E47Q advances only if all four checks pass.
