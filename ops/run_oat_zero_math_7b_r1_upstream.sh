#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
UPSTREAM_ROOT="${OAT_ZERO_UPSTREAM_ROOT:-$ROOT_DIR/understand-r1-zero}"

export RUN_STAMP="${RUN_STAMP:-$(date +%Y%m%d_%H%M%S)}"
export SAVE_PATH="${SAVE_PATH:-$ROOT_DIR/var/data/oat_zero_math_7b_r1_${RUN_STAMP}}"
export OAT_ZERO_WB_PROJECT="${OAT_ZERO_WB_PROJECT:-oat-zero}"
export OAT_ZERO_WB_RUN_NAME="${OAT_ZERO_WB_RUN_NAME:-qwen2.5-Math-7b-drgrpo-r1template-readmeflash-${RUN_STAMP}}"
export OAT_ZERO_PRETRAIN="${OAT_ZERO_PRETRAIN:-Qwen/Qwen2.5-Math-7B}"
export OAT_ZERO_PROMPT_TEMPLATE="${OAT_ZERO_PROMPT_TEMPLATE:-r1}"
export OAT_ZERO_VERIFIER_VERSION="${OAT_ZERO_VERIFIER_VERSION:-math_verify}"
export OAT_ZERO_PROMPT_DATA="${OAT_ZERO_PROMPT_DATA:-$UPSTREAM_ROOT/datasets/train/math_lvl3to5_8k}"
export OAT_ZERO_EVAL_DATA="${OAT_ZERO_EVAL_DATA:-$UPSTREAM_ROOT/datasets/evaluation_suite}"
export OAT_ZERO_INPUT_KEY="${OAT_ZERO_INPUT_KEY:-problem}"
export OAT_ZERO_OUTPUT_KEY="${OAT_ZERO_OUTPUT_KEY:-answer}"
export OAT_ZERO_EVAL_STEPS="${OAT_ZERO_EVAL_STEPS:-16}"
export OAT_ZERO_NUM_SAMPLES="${OAT_ZERO_NUM_SAMPLES:-8}"
export OAT_ZERO_TRAIN_BATCH_SIZE="${OAT_ZERO_TRAIN_BATCH_SIZE:-128}"
export OAT_ZERO_TRAIN_BATCH_SIZE_PER_DEVICE="${OAT_ZERO_TRAIN_BATCH_SIZE_PER_DEVICE:-1}"
export OAT_ZERO_ROLLOUT_BATCH_SIZE="${OAT_ZERO_ROLLOUT_BATCH_SIZE:-128}"
export OAT_ZERO_ROLLOUT_BATCH_SIZE_PER_DEVICE="${OAT_ZERO_ROLLOUT_BATCH_SIZE_PER_DEVICE:-16}"
export OAT_ZERO_PI_BUFFER_MAXLEN_PER_DEVICE="${OAT_ZERO_PI_BUFFER_MAXLEN_PER_DEVICE:-128}"
export OAT_ZERO_VLLM_GPU_RATIO="${OAT_ZERO_VLLM_GPU_RATIO:-0.35}"
export OAT_ZERO_BETA="${OAT_ZERO_BETA:-0}"
export OAT_ZERO_LEARNING_RATE="${OAT_ZERO_LEARNING_RATE:-0.000001}"
export OAT_ZERO_NUM_PPO_EPOCHS="${OAT_ZERO_NUM_PPO_EPOCHS:-1}"

exec "$ROOT_DIR/ops/run_oat_zero_exact_1p5b_upstream.sh" "$@"
