name: Perf

on:
  push:
    branches: [ main, master, develop, dev, '**' ]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  perf:
    runs-on: ubuntu-latest
    env:
      PYTHONNOUSERSITE: "1"
      PYTHONPATH: ./src
      XDG_CACHE_HOME: ${{ github.workspace }}/var/cache/xdg
      PIP_CACHE_DIR: ${{ github.workspace }}/var/cache/pip
      TMPDIR: ${{ github.workspace }}/var/tmp
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install project (no deps)
        run: |
          mkdir -p "$XDG_CACHE_HOME" "$PIP_CACHE_DIR" "$TMPDIR"
          python -m pip install --upgrade pip
          python -m pip install -e . --no-deps

      - name: Run microbench
        run: python tools/perf_microbench.py --iterations 500 --baseline-file tools/perf_baseline.json --tolerance 0.35 --json-output perf_metrics.json

      - name: Upload perf metrics
        uses: actions/upload-artifact@v4
        with:
          name: perf-metrics
          path: perf_metrics.json
